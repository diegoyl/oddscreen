import './ComponentName.css';

function ComponentName({props}) {

    // do stuff with props

    return (
        <div className="">
            
        </div>
    );
}
  
export default ComponentName;